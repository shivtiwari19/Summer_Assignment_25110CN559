#include <stdio.h>

int main() {
    int n, i, sum = 0, total = 0, a[100];

    scanf("%d", &n);

    for(i = 0; i < n-1; i++) {
        scanf("%d", &a[i]);
        sum += a[i];
    }

    total = n * (n + 1) / 2;

    printf("Missing number = %d", total - sum);

    return 0;
}