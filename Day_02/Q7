#include<stdio.h>
int main () {
    int n,d,mul=1;
    printf("Enter a no.:");
    scanf("%d",&n);

    while(n>0) {
        d=n%10;
        mul=mul*d;
        n=n/10;
    }
    printf("The result is :%d",mul);
    return 0;
}