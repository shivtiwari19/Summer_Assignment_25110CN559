#include <stdio.h>

int main() {
    char str[100], rev[100];
    int i, len = 0, flag = 1;

    printf("Enter a string: ");
    gets(str);

    while (str[len] != '\0')
        len++;

    for (i = 0; i < len; i++) {
        rev[i] = str[len - 1 - i];
    }
    rev[i] = '\0';

    for (i = 0; i < len; i++) {
        if (str[i] != rev[i]) {
            flag = 0;
            break;
        }
    }

    if (flag)
        printf("Palindrome String");
    else
        printf("Not a Palindrome String");

    return 0;
}